# Handoff 02 → 03

## Resultado

M2 foi concluído localmente em 2026-08-13. Nenhum runtime Prime Agent, modelo,
rede, dependência nova ou trabalho do M3 foi iniciado.

## Arquivos entregues

- `.prime/agent/skills/article-loop/src/article_loop/state_machine.py`
- `.prime/agent/skills/article-loop/src/article_loop/store.py`
- `control/test_m2_durable_state.py`
- `.prime/agent/skills/article-loop/SKILL.md`

## API pública

`article_loop` exporta `DurableStore`, `State`, `TransitionError`,
`StoreError`, `IntegrityError` e `StopRequested`. `DurableStore` fornece
`create_run`, `record`, `read_events`, `snapshot`, `rebuild_snapshot`,
`checkpoint`, `pause` e `resume`. `run()` continua um stub explícito até M6.

## Invariantes

- `run_id` é identificador simples e não permite path traversal.
- JSONL é a fonte de verdade; cada evento tem sequência, hash anterior e hash
  canônico, IDs e chaves de idempotência únicos.
- JSONL parcial/truncado, JSON inválido ou hash/cadeia divergentes falham com
  `IntegrityError`; não há reparo silencioso.
- snapshots e checkpoints são derivados do replay; snapshots usam escrita
  temporária, flush, fsync, rename e verificação de hash.
- lock exclusivo serializa escritores por `run_id`; `control/STOP` é verificado
  após adquirir o lock e bloqueia novas operações.
- pausa preserva exatamente o estado retomável; `FINALIZED` e
  `TECHNICAL_FAILURE` são terminais.

## Testes executados

- `.venv/bin/python -m unittest -v control.test_contracts` — 46 testes OK.
- `.venv/bin/python -m unittest -v control.test_m2_durable_state` — 13 testes
  OK: transições, replay, duplicidade, truncamento/corrupção, crashes antes e
  depois de rename, concorrência, hash, snapshot, checkpoint, pause/resume,
  STOP, traversal e reinício idempotente.
- `.venv/bin/python -m unittest discover -v` — 59 testes OK.

## Pendências

M3 permanece pendente e exige PDF de entrada e o procedimento de preservação
de hash definido no plano. Não há pendência corretiva aberta no escopo de M2.
