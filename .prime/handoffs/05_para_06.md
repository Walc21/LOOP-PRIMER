# Handoff M5 → M6

## Estado alcançado

M5 foi corrigido cirurgicamente e está concluído. M6 não foi iniciado. Não
houve chamadas a modelos, Prime Agent, rede ou agentes.

## Contrato corrigido

- `Blackboard.impact()` normaliza `id`, `kind` e todos os localizadores
  estruturados sem escolher um campo em detrimento de outro. Preserva seção,
  equação e referência e fecha dependências direta e indiretamente.
- `state/blackboard/<run_id>/<ledger>.jsonl` aceita somente `run_id` simples,
  recusa symlinks em caminhos gerenciados e confina a raiz validada. O append
  usa lock exclusivo por execução, valida conflito/idempotência sob o lock e
  grava JSONL completo com flush/fsync; a leitura recusa linhas parciais.
- O planejador usa severidade, idade, dependências alteradas, falhas e sinais
  de plateau/oscilação/diagnóstico. `RUN` cobre trabalho novo/alta severidade,
  `CHECK` cobre validação (inclusive W22 para teorema/prova), `SHIFT` cobre
  plateau, oscilação, diagnóstico ou falhas repetidas, e `FREEZE` tem custo,
  tempo, entradas e saídas vazios.
- W22 é obrigatório para teorema/prova; W51 e W53 são obrigatórios na
  finalização. Limites por ciclo, departamento, gerente, papel, tokens ou
  tempo não truncam cobertura: retornam `paused` com checkpoint explícito.
- `manager_view` recebe exatamente S10, S20, S30, S40 e S50, uma vez cada e
  nessa ordem. Cada pacote é `COMPLETE` com proposta-objeto ou `NO_CHANGE`
  fechado (`department_id`, `status`).

## Arquivos relevantes

- `PLANS.md` e `docs/decisions.md` (ADR-021): plano e decisão corrigidos.
- `.prime/agent/skills/article-loop/src/article_loop/blackboard.py`: memória,
  confinamento e grafo de impacto.
- `.prime/agent/skills/article-loop/src/article_loop/activation.py`: seleção,
  modos, limites e views.
- `control/test_m5_blackboard_activation.py`: casos adversariais M5.

## Validação executada

```text
.venv/bin/python -m unittest -v control.test_m5_blackboard_activation
13 testes passaram.

.venv/bin/python -m unittest discover -s control -p 'test_*.py'
124 testes passaram.
```

Ambas as execuções foram locais. A suíte integral emitiu somente o aviso
esperado do teste M3 sobre entrada ZIP duplicada. Não iniciar M6 nesta sessão.
